#include <stdio.h>

_Bool printHere(int N){
    int flag=0;
    
    for(int i=1;i<=N;i++){
        if(i*i==N){
            return 1;
        }
    }
     return 0;
}

int main()
{
    int N;
    scanf("%d", &N);
    printf("%d", printHere(N));
}
